<nav class=" bg-dark navbar navbar-expand-lg bg-white rounded-bottom-3">
    <!-- Logo -->
    <a class="navbar-brand fw-bold" href="/">
      Avito Clone
    </a>

    <!-- Toggle button for mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar content -->
    <div class="collapse navbar-collapse" id="navbarContent">
      <!-- Left side: Categories -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% Category.all.each do |category| %>
          <li class="nav-item opacity-75">
            <%= link_to category.name.capitalize, annoncements_path(category: category.id, name: category.name.capitalize), class: "nav-link text-black" %>
          </li>
        <% end %>
      </ul>



      <!-- Right side: Buttons -->
      <% if user_signed_in? %>
        <!-- If signed in -->
        <%= link_to "Publier une annonce", new_annoncement_path, class: "btn btn-danger ms-3" %>
        <%= button_to destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary ms-2" do %>
          Se Déconnecter
        <% end %>
      <% else %>
        <!-- If not signed in -->
        <%= link_to "Se Connecter", new_user_session_path, class: "btn btn-outline-secondary ms-2" %>
        <%= link_to "Publier une annonce", new_user_session_path, class: "btn btn-danger ms-3" %>
      <% end %>

    </div>
</nav>
